# Copia este archivo a ".env" (en la raíz del proyecto, junto a package.json).
# Solo variables públicas van aquí: todo lo que empieza con VITE_ termina en el
# bundle del navegador, así que nunca pongas secretos en este archivo.

# Site key pública de Google reCAPTCHA usada en el login de la intranet
# (la secret key correspondiente vive únicamente en la API central de BBBSC).
VITE_RECAPTCHA_SITE_KEY=6LfV-VgtAAAAAJyQZ9Z30OtCSjtzod4d1mDW3JyK
